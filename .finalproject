% set variables to images
Dough = imread('dough.png');
Sauce = imread('sauce.png');
Cheese = imread('cheese.png');
Pepperoni = imread('pepperoni.png');
Mushroom = imread('mushroom.png');
pizzaToppings = [] %creates empty array of toppings
amtToppings = 0;

for i=1:100 %iterates through dice rolls until there are 6 elements in the array
   
   while(1) 
       
        if length(pizzaToppings) == 6 %for loop breaks if there are 6 toppings
       break
   end
   
   diceRoll = randi([1,6],1,1) %generates a random number between 1 and 6
   
if diceRoll == 1 
    amtToppings = amtToppings + 1
    pizzaToppings(amtToppings) = 1

elseif diceRoll == 2
    amtToppings = amtToppings + 1
    pizzaToppings(amtToppings) = 2

elseif diceRoll == 3
    amtToppings = amtToppings + 1
    pizzaToppings(amtToppings) = 3

elseif diceRoll == 4
    amtToppings = amtToppings + 1
    pizzaToppings(amtToppings) = 4

elseif diceRoll == 5
    amtToppings = amtToppings + 1
    pizzaToppings(amtToppings) = 5

elseif diceRoll == 6
    if amtToppings == 0
        disp("No Toppings")
    else
        pizzaToppings(amtToppings) = [] %deletes the most current topping
        amtToppings = amtToppings - 1
    end
   
end

m=input('Do you want to continue, Y/N [Y]:','s') %asks for user input to continue 
if m=='N'
break
end
    end
             end

% scoring
    if length(pizzaToppings) == 5 
        app.ScoreEditField = 50;
    elseif length(pizzaToppings) == 4 
        app.ScoreEditField = 40;
    elseif length(pizzaToppings) == 3 
        app.ScoreEditField = 30;
    elseif length(pizzaToppings) == 2 
        app.ScoreEditField = 20;
    elseif length(pizzaToppings) == 1 
        app.ScoreEditField = 10;
    else length(pizzaToppings) == 0 
        app.ScoreEditField == 0;
    
